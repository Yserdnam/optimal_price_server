<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Optimisation de Prix - <%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-chart-boxplot"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-chart-matrix"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <style>
        :root {
            --primary-color: #4e73df;
            --secondary-color: #6f42c1;
            --text-color: #333;
            --hover-color: #2e59d9;
            --background-color: #f8f9fc;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: var(--background-color);
            padding-top: 80px;
        }
        
        .navbar-custom {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
            padding: 0.8rem 1rem;
        }
        
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: white !important;
            display: flex;
            align-items: center;
        }
        
        .navbar-brand i {
            margin-right: 10px;
            font-size: 1.8rem;
        }
        
        .nav-link {
            color: rgba(255, 255, 255, 0.85) !important;
            font-weight: 500;
            padding: 0.8rem 1rem !important;
            margin: 0 5px;
            border-radius: 6px;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
        }
        
        .nav-link i {
            margin-right: 8px;
            font-size: 1.2rem;
        }
        
        .nav-link:hover, .nav-link.active {
            background-color: rgba(255, 255, 255, 0.15);
            color: white !important;
            transform: translateY(-2px);
        }
        
        .navbar-toggler {
            border: none;
            color: white !important;
        }
        
        .navbar-toggler:focus {
            box-shadow: none;
        }
        
        .content-section {
            background: white;
            border-radius: 10px;
            padding: 25px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.05);
            margin-bottom: 30px;
        }
        
        h2 {
            color: var(--primary-color);
            border-bottom: 2px solid var(--primary-color);
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        
        .badge-custom {
            background: var(--secondary-color);
            color: white;
        }
        
        @media (max-width: 991.98px) {
            .nav-link {
                margin: 3px 0;
            }
        }

        .card { margin-bottom: 20px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        .stat-card { text-align: center; padding: 20px; }
        .stat-number { font-size: 2rem; font-weight: bold; }
        .stat-label { font-size: 0.8rem; color: #6c757d; }
        .chart-container { position: relative; height: 400px; margin-bottom: 20px; }
        .section-title { border-left: 4px solid #0d6efd; padding-left: 15px; margin: 30px 0 20px 0; }
        .loading { text-align: center; padding: 50px; color: #6c757d; }

    </style>
</head>
<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="#">
                <i class="fas fa-utensils"></i>
                Lasopy
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link <%= title === 'Dashboard' ? 'active' : '' %>" href="/dashboard">
                            <i class="fas fa-tachometer-alt"></i>
                            Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= title === 'Ingredients' ? 'active' : '' %>" href="/ingredients">
                            <i class="fas fa-carrot"></i>
                            Ingrédients
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= title === 'Soupes' ? 'active' : '' %>" href="/soupes">
                            <i class="fas fa-utensil-spoon"></i>
                            Soupes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= title === 'Concurrents' ? 'active' : '' %>" href="/concurrents">
                            <i class="fas fa-store"></i>
                            Concurrents
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= title === 'Quartier' ? 'active' : '' %>" href="/quartiers">
                            <i class="fas fa-map-marker-alt"></i>
                            Quartier
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link <%= title === 'Pricing' ? 'active' : '' %>" href="/pricing">
                            <i class="fas fa-tag"></i>
                            Pricing
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<main class="container pb-5">
